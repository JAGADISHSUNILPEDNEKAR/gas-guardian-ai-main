{
  "name": "gasguard-contracts",
  "version": "1.0.0",
  "description": "GasGuard Mentor Smart Contracts",
  "scripts": {
    "compile": "hardhat --config ../hardhat.config.cjs compile",
    "test": "hardhat --config ../hardhat.config.cjs test",
    "deploy:coston2": "hardhat --config ../hardhat.config.cjs run ../scripts/deploy/deployGasGuard.cjs --network coston2",
    "deploy:flare": "hardhat --config ../hardhat.config.cjs run ../scripts/deploy/deployGasGuard.cjs --network flare",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}

